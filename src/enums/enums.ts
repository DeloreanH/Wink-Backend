export enum jwtAlgorithm {
    HS256 = 'HS256',
    HS384 = 'HS384',
    HS512 = 'HS512',
    RS256 = 'RS256',
}

export enum Meaning {
    SECONDS = 'seconds',
    MINUTES = 'minutes',
    HOURS   = 'hours',
    DAYS    = 'days',
    MONTHS  = 'months',
}
